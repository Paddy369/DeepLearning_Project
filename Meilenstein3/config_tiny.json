{
    "model_id": "model_tiny",
    "batch_size_train": 32,
    "batch_size_val": 8,
    "batch_size_test": 8,
    "batch_size_predict": 8,
    "epochs": 10,
    "image_size": 256,
    "augmentation": false,
    "learning_rate": {
        "decay": true,
        "initial_lr": 0.01,
        "steps": 100,
        "decay_rate": 1 
    }
}